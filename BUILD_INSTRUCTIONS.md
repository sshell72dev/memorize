# Инструкции по сборке и запуску приложения Memorize

## Android

### Требования
- Android Studio Hedgehog (2023.1.1) или новее
- JDK 17 или выше
- Android SDK (API 24+)
- Gradle 8.2+

### Шаги сборки

1. **Откройте проект в Android Studio:**
   ```bash
   # Откройте Android Studio
   # File -> Open -> выберите папку android/
   ```

2. **Настройте API ключи:**
   - Скопируйте `android/config.xml.example` в `android/app/src/main/res/values/config.xml`
   - Замените `YOUR_YANDEX_API_KEY` и `YOUR_FOLDER_ID` на ваши значения

3. **Синхронизируйте Gradle:**
   - Android Studio автоматически предложит синхронизировать проект
   - Или: File -> Sync Project with Gradle Files

4. **Соберите проект:**
   ```bash
   # Через командную строку:
   cd android
   ./gradlew assembleDebug
   
   # Или через Android Studio:
   # Build -> Make Project
   ```

5. **Запустите на эмуляторе или устройстве:**
   ```bash
   # Через командную строку:
   ./gradlew installDebug
   
   # Или через Android Studio:
   # Run -> Run 'app'
   ```

### Тестирование Android

1. **Запустите приложение**
2. **Протестируйте поиск:**
   - Введите название текста (например, "Евгений Онегин")
   - Нажмите "Найти и начать учить"
   - Дождитесь загрузки и парсинга текста

3. **Протестируйте загрузку файла:**
   - Нажмите "📁 Загрузить файл"
   - Выберите текстовый файл (.txt)
   - Проверьте отображение содержимого
   - Введите название и сохраните

4. **Протестируйте ввод текста:**
   - Нажмите "✏️ Ввести текст"
   - Введите название и текст
   - Сохраните

5. **Протестируйте обучение:**
   - Выберите текст для обучения
   - Проверьте первый прогон (бот читает → вы повторяете)
   - Проверьте второй прогон (вы говорите первым)
   - Проверьте кумулятивный повтор
   - Проверьте переходы между абзацами

6. **Проверьте статистику:**
   - После завершения обучения проверьте экран статистики
   - Убедитесь, что отображается время, повторы, ошибки, оценка

## iOS

### Требования
- Xcode 15.0 или новее
- macOS (для сборки)
- CocoaPods
- iOS 17.0+

### Шаги сборки

1. **Установите зависимости:**
   ```bash
   cd ios
   pod install
   ```

2. **Настройте API ключи:**
   - Откройте `ios/Memorize/Config.swift`
   - Замените `YOUR_YANDEX_API_KEY` и `YOUR_FOLDER_ID` на ваши значения

3. **Откройте проект в Xcode:**
   ```bash
   open ios/Memorize.xcworkspace
   # НЕ используйте .xcodeproj, только .xcworkspace!
   ```

4. **Настройте подписи:**
   - Выберите проект в навигаторе
   - Перейдите в Signing & Capabilities
   - Выберите вашу команду разработчика

5. **Выберите устройство или симулятор:**
   - В верхней панели Xcode выберите устройство

6. **Соберите и запустите:**
   - Нажмите Cmd+R или кнопку Run
   - Или через командную строку:
   ```bash
   xcodebuild -workspace ios/Memorize.xcworkspace \
     -scheme Memorize \
     -sdk iphonesimulator \
     -destination 'platform=iOS Simulator,name=iPhone 15' \
     build
   ```

### Тестирование iOS

Те же шаги, что и для Android:
1. Поиск текста через AI
2. Загрузка файла
3. Ввод текста вручную
4. Процесс обучения
5. Статистика

## Возможные проблемы

### Android

1. **Ошибка компиляции Room:**
   - Убедитесь, что `kotlin-kapt` плагин добавлен
   - Clean Project: Build -> Clean Project
   - Rebuild: Build -> Clean Project, затем Build -> Make Project

2. **Ошибки импорта:**
   - Синхронизируйте Gradle
   - Invalidate Caches: File -> Invalidate Caches / Restart

3. **Проблемы с разрешениями:**
   - Убедитесь, что в AndroidManifest.xml есть разрешения:
     - `INTERNET`
     - `RECORD_AUDIO`

### iOS

1. **Ошибки Pod:**
   ```bash
   cd ios
   pod deintegrate
   pod install
   ```

2. **Проблемы с подписями:**
   - Выберите автоматическую подпись в Xcode
   - Или настройте вручную в Signing & Capabilities

3. **Ошибки компиляции Swift:**
   - Проверьте версию Swift (должна быть 5.0+)
   - Clean Build Folder: Cmd+Shift+K

## Проверка функциональности

### Обязательные тесты:

1. ✅ Поиск текста через Yandex GPT
2. ✅ Загрузка текста из файла
3. ✅ Ввод текста вручную
4. ✅ Парсинг текста на разделы/абзацы/фразы
5. ✅ Первый прогон обучения
6. ✅ Второй прогон обучения
7. ✅ Кумулятивный повтор
8. ✅ Переходы между абзацами и разделами
9. ✅ Распознавание речи
10. ✅ Text-to-Speech
11. ✅ Сохранение прогресса в БД
12. ✅ Экран статистики
13. ✅ Обработка ошибок

### Тестовые данные:

Для тестирования можно использовать:
- Короткое стихотворение (4-8 строк)
- Отрывок из прозы
- Собственный текст

