# Быстрый старт: Сборка и запуск приложений

## 📱 Android - Подробная инструкция

### Вариант 1: Через Android Studio (Рекомендуется)

#### Шаг 1: Установка Android Studio
1. Скачайте Android Studio с [официального сайта](https://developer.android.com/studio)
2. Установите Android Studio
3. При первом запуске выберите "Standard" установку
4. Дождитесь загрузки Android SDK и компонентов

#### Шаг 2: Открытие проекта
1. Запустите Android Studio
2. Выберите **File → Open** (или **Open an Existing Project**)
3. Перейдите в папку `D:\sites\memorize\android`
4. Нажмите **OK**

#### Шаг 3: Первая синхронизация
1. Android Studio автоматически обнаружит проект Gradle
2. Внизу появится уведомление "Gradle files have changed since last project sync"
3. Нажмите **Sync Now** или **File → Sync Project with Gradle Files**
4. ⏳ **Это займет 5-15 минут** при первом запуске:
   - Скачается Gradle wrapper
   - Загрузятся все зависимости (Compose, Room, Retrofit и т.д.)
   - Установятся Android SDK компоненты

#### Шаг 4: Настройка эмулятора (если нет устройства)
1. Нажмите на иконку **Device Manager** в правой панели (или **Tools → Device Manager**)
2. Нажмите **Create Device**
3. Выберите устройство (например, **Pixel 5**)
4. Выберите системный образ (рекомендуется **API 34** или **API 33**)
5. Нажмите **Download** если образ не установлен
6. Завершите создание эмулятора

#### Шаг 5: Сборка проекта
1. В верхнем меню выберите **Build → Make Project** (или нажмите `Ctrl+F9`)
2. Дождитесь завершения сборки (первые раз может занять 2-5 минут)
3. Внизу в панели **Build** вы увидите прогресс

#### Шаг 6: Запуск приложения
1. Убедитесь, что эмулятор запущен или устройство подключено:
   - Для эмулятора: выберите его в списке устройств и нажмите ▶️
   - Для устройства: включите **Режим разработчика** и **Отладка по USB**
2. В Android Studio нажмите зеленую кнопку **Run** (▶️) или `Shift+F10`
3. Выберите устройство из списка
4. Приложение установится и запустится

### Вариант 2: Через командную строку

#### Предварительные требования
- Установлен JDK 17 или выше
- Установлен Android SDK (через Android Studio или отдельно)
- Переменная окружения `ANDROID_HOME` настроена

#### Шаг 1: Скачать Gradle Wrapper JAR
```powershell
# Перейдите в папку проекта
cd D:\sites\memorize\android

# Создайте папку если её нет
New-Item -ItemType Directory -Force -Path "gradle\wrapper"

# Скачайте gradle-wrapper.jar
Invoke-WebRequest -Uri "https://raw.githubusercontent.com/gradle/gradle/v8.2.0/gradle/wrapper/gradle-wrapper.jar" -OutFile "gradle\wrapper\gradle-wrapper.jar"
```

#### Шаг 2: Сборка через командную строку
```powershell
# Соберите debug APK
.\gradlew.bat assembleDebug

# Или соберите и установите на подключенное устройство
.\gradlew.bat installDebug
```

#### Шаг 3: Запуск на устройстве
```powershell
# Убедитесь, что устройство подключено
adb devices

# Запустите приложение
adb shell am start -n com.memorize/.MainActivity
```

### Возможные проблемы Android

#### Проблема: "Gradle sync failed"
**Решение:**
1. Проверьте интернет-соединение
2. **File → Invalidate Caches / Restart → Invalidate and Restart**
3. Удалите папку `.gradle` в папке проекта
4. Попробуйте снова синхронизировать

#### Проблема: "SDK location not found"
**Решение:**
1. **File → Project Structure → SDK Location**
2. Укажите путь к Android SDK (обычно `C:\Users\YourName\AppData\Local\Android\Sdk`)

#### Проблема: "Failed to resolve: androidx.compose..."
**Решение:**
1. Проверьте интернет-соединение
2. **File → Settings → Build, Execution, Deployment → Gradle**
3. Убедитесь, что используется правильная версия Gradle

---

## 🍎 iOS - Подробная инструкция

**⚠️ ВАЖНО:** iOS приложения можно собирать **только на macOS** с установленным Xcode!

### Шаг 1: Установка Xcode
1. Откройте **App Store** на Mac
2. Найдите и установите **Xcode** (требуется macOS 13.0+)
3. После установки запустите Xcode и примите лицензионное соглашение
4. Установите дополнительные компоненты при запросе

### Шаг 2: Установка CocoaPods
```bash
# Откройте Terminal на Mac
sudo gem install cocoapods
```

### Шаг 3: Установка зависимостей
```bash
# Перейдите в папку iOS проекта
cd /path/to/memorize/ios

# Установите зависимости
pod install
```

**Ожидаемый вывод:**
```
Analyzing dependencies
Downloading dependencies
Installing Alamofire (5.8.x)
Installing SQLite.swift (0.15.x)
Generating Pods project
Pod installation complete!
```

### Шаг 4: Открытие проекта в Xcode
1. **НЕ открывайте** `Memorize.xcodeproj`!
2. Откройте **`Memorize.xcworkspace`**:
   ```bash
   open Memorize.xcworkspace
   ```
   Или через Finder: найдите файл `Memorize.xcworkspace` и дважды кликните

### Шаг 5: Настройка подписей
1. В Xcode выберите проект **Memorize** в левой панели
2. Выберите target **Memorize**
3. Перейдите на вкладку **Signing & Capabilities**
4. Поставьте галочку **Automatically manage signing**
5. Выберите вашу **Team** (Apple ID)
6. Xcode автоматически создаст provisioning profile

### Шаг 6: Выбор устройства/симулятора
1. В верхней панели Xcode найдите выпадающий список устройств
2. Выберите:
   - **iPhone 15** (или другой симулятор) для тестирования на симуляторе
   - Ваше физическое устройство (если подключено)

### Шаг 7: Сборка и запуск
1. Нажмите **⌘ + R** (Cmd + R) или кнопку **▶️ Run** в левом верхнем углу
2. Xcode соберет проект (первый раз может занять 2-5 минут)
3. Приложение установится и запустится на выбранном устройстве/симуляторе

### Альтернатива: Сборка через командную строку
```bash
# Перейдите в папку iOS проекта
cd /path/to/memorize/ios

# Соберите проект
xcodebuild -workspace Memorize.xcworkspace \
  -scheme Memorize \
  -sdk iphonesimulator \
  -destination 'platform=iOS Simulator,name=iPhone 15' \
  build

# Запустите на симуляторе
xcodebuild -workspace Memorize.xcworkspace \
  -scheme Memorize \
  -sdk iphonesimulator \
  -destination 'platform=iOS Simulator,name=iPhone 15' \
  test
```

### Возможные проблемы iOS

#### Проблема: "No such module 'Alamofire'"
**Решение:**
```bash
cd ios
pod deintegrate
pod install
```
Затем закройте и снова откройте `.xcworkspace`

#### Проблема: "Signing for Memorize requires a development team"
**Решение:**
1. В Xcode: **Project → Signing & Capabilities**
2. Выберите вашу Team или добавьте Apple ID в **Xcode → Settings → Accounts**

#### Проблема: "Could not find module 'SQLite'"
**Решение:**
1. Убедитесь, что открыт `.xcworkspace`, а не `.xcodeproj`
2. Выполните `pod install` заново

---

## 🧪 Проверка работоспособности

После успешного запуска приложения проверьте:

### Android
1. ✅ Приложение открывается
2. ✅ Экран поиска отображается
3. ✅ Кнопки "Загрузить файл" и "Ввести текст" видны
4. ✅ При первом использовании запрашиваются разрешения на микрофон

### iOS
1. ✅ Приложение открывается
2. ✅ Экран поиска отображается
3. ✅ Кнопки "Загрузить файл" и "Ввести текст" видны
4. ✅ При первом использовании запрашиваются разрешения на микрофон и распознавание речи

---

## 📝 Следующие шаги

После успешного запуска:
1. Протестируйте функциональность согласно `TESTING_CHECKLIST.md`
2. Попробуйте загрузить текст через AI
3. Протестируйте процесс обучения

---

## 💡 Полезные команды

### Android
```powershell
# Очистить проект
.\gradlew.bat clean

# Собрать release версию
.\gradlew.bat assembleRelease

# Просмотреть подключенные устройства
adb devices

# Просмотреть логи
adb logcat
```

### iOS
```bash
# Очистить сборку
xcodebuild clean -workspace Memorize.xcworkspace -scheme Memorize

# Обновить зависимости
cd ios && pod update && cd ..

# Просмотреть логи симулятора
xcrun simctl spawn booted log stream
```

---

## ❓ Нужна помощь?

Если возникли проблемы:
1. Проверьте `BUILD_INSTRUCTIONS.md` для детальной информации
2. Убедитесь, что все зависимости установлены
3. Проверьте версии инструментов (Android Studio, Xcode, Gradle)
4. Посмотрите логи ошибок в консоли


